# Copy this to .env on your production server and fill in values
# Generate secrets with: openssl rand -base64 32

# Domain (point DNS A record to your server IP first)
DOMAIN=api.yourdomain.com

DATABASE_NAME=pmcnews
DATABASE_USERNAME=strapi
DATABASE_PASSWORD=CHANGE_ME_strong_password_here

APP_KEYS=key1_base64,key2_base64
API_TOKEN_SALT=CHANGE_ME
ADMIN_JWT_SECRET=CHANGE_ME
TRANSFER_TOKEN_SALT=CHANGE_ME
JWT_SECRET=CHANGE_ME
ENCRYPTION_KEY=CHANGE_ME
